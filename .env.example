# Optional environment variables referenced by local provider profiles.
# Set secrets in your shell or local ignored .env; never commit actual values.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=example-model-name
